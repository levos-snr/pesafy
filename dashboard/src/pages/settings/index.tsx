/**
 * pages/settings/index.tsx — /settings/*
 *
 * Thin shell: renders the correct sub-page based on the route.
 * The sidebar handles navigation (just like Finance/Analytics/Sales).
 * No tab bar needed here — sub-pages are top-level routes.
 */
export { default } from "./GeneralSettings";
